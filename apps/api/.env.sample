# Supabase Credentials
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_SONGS_BUCKET=songs

# API Settings
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Instructions:
# 1. Rename this file to .env.development or .env.local
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. The service role key can be found in your Supabase project dashboard under Settings > API
# 4. Never share your service key or commit it to version control 